#ifndef TOKENIZER_HPP
#define TOKENIZER_HPP

#include <string>
#include <vector>

// Deklariere eine strikt funktionale Tokenisierungsfunktion
std::vector<std::string> tokenize(const std::string& text);

#endif // TOKENIZER_HPP
